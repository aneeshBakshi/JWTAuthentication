import express from "express";

const loginRouter = express.Router();

/**
 * @swagger
 * tags:
 *   name: User Login
 *   description: Endpoints for User Login management
 */
export { loginRouter };
